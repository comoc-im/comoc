import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
    appId: 'fun.chummy.comoc',
    appName: 'comoc',
    webDir: 'dist',
    bundledWebRuntime: false,
}

export default config
